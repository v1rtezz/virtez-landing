# Доработки

1. Добавить аналитику кликов по CTA (`hero`, `trust`, `sticky`) с событиями в GA/Метрику.
2. Передавать source в ссылке (`start=landing_hero`, `landing_sticky`) для точной атрибуции конверсии.
3. Исправить предупреждение Vite про `styles.css` (сейчас `link` в `index.html` лишний при импорте стилей из `main.js`).
4. Для A/B-теста добавить сбор метрик и определение победителя, иначе тест только меняет тексты.
5. Оптимизировать ассеты (сжатие, lazy-loading), чтобы улучшить LCP на мобильных.
